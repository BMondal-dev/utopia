info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/v1/account/sessions/email
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "bijitju@bijit.xyz",
        "password": "bijitju@bijit.xyz"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
